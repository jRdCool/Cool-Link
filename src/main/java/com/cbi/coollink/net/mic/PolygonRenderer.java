package com.cbi.coollink.net.mic;

import net.minecraft.client.render.VertexConsumer;
import net.minecraft.client.util.math.MatrixStack;
import net.minecraft.util.math.Vec3d;
import net.minecraft.world.World;
import org.joml.Matrix4f;

public class PolygonRenderer {



    /*public PolygonRenderer() {}

    private void renderLine(World world, MatrixStack matrixStack, VertexConsumer buffer, Vec3d start, Vec3d end) {
        Matrix4f matrix = matrixStack.peek().getPositionMatrix();
        buffer.(matrix, (float)start.x, (float)start.y, (float)start.z, (float)end.x, (float)end.y, (float)end.z)
                .next();
    }*/




}
